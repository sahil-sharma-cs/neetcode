/**
-the key is to use a map to keep index number and target - sum = map contains
-how you return the order of the result array is imp. return already existing number first (ie map value) 
 and then current index
**/

class temp {

    public static int[] twoSum(int[] arr, int sum) {

        Map<Integer, Integer>  map = new HashMap<>();

        int result[] = new int[2];

        for(int i=0;i<arr.length;i++) {
            if(map.containsKey(sum - arr[i])) {
                result[0] = map.get(sum - arr[i]);
                result[1] = i;
                return result;
            } else {
                map.put(arr[i], i);
            }
        }
        return result;
    }

    public static void main(String[] args) {
        int arr[] = twoSum(new int[]{3,4,5,6},7);
        System.out.println(arr[0] + " " + arr[1]);
    }


}
